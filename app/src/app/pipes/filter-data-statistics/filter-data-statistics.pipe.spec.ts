import FilterDataStatisticsPipe from './filter-data-statistics.pipe';

describe('FilterDataStatisticsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDataStatisticsPipe();
    expect(pipe).toBeTruthy();
  });
});
